# API Configuration
# The base URL for your API endpoint (no trailing slash)
# Must use HTTPS for security
EXPO_PUBLIC_API_BASE=https://your-api-domain.com

# Admin Authentication
# Secure token for accessing /admin/* routes
# Generate a secure random string (minimum 32 characters recommended)
# Example: openssl rand -hex 32
ADMIN_TOKEN=your-secure-admin-token-here
